Exp 8
Install Docker and execute docker commands to manage images and interact with containers

sudo apt install docker.io
docker --version
sudo systemctl start docker
sudo systemctl enable docker
sudo systemctl status docker
sudo docker ps
sudo docker ps -a
Or
sudo docker container ps -a
sudo docker images
sudo docker run hello-world
sudo docker pull ubuntu
sudo docker run hello-world
sudo docker images

sudo docker login -u pranjali12
Then enter password for docker login
sudo docker tag ubuntu:latest pranjali12/new_img:latest
sudo docker push pranjali12/new_img:latest
Then in your docker account new_img will be added
